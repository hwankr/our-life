# OurLife - 친구와 함께 목표를 추적하는 웹앱을 바이브 코딩으로 만들어보았다

> 카테고리: 바이브 코딩 > our-life

---

## 프로젝트 소개

"친구랑 같이 6개월 동안 목표를 세우고 서로 체크하면 어떨까?"

이 단순한 아이디어에서 **OurLife** 프로젝트가 시작되었다. OurLife는 두 명이 함께 일정 기간 동안 목표를 설정하고, 매일 기록하며, 서로의 진행 상황을 확인할 수 있는 웹 애플리케이션이다.

혼자 하면 작심삼일이 되기 쉬운 목표도, 친구와 함께라면 다르다. 서로의 기록을 투명하게 공유함으로써 자연스러운 동기부여가 되는 것이 핵심 컨셉이다.

<!-- 📸 이미지 제안: 랜딩 페이지 스크린샷 (라이트 모드 + 다크 모드 병렬 배치) -->

---

## 기술 스택

이 프로젝트는 바이브 코딩답게 AI와 대화하면서 빠르게 만들어냈다. 사용한 기술 스택은 다음과 같다.

| 영역 | 기술 |
|------|------|
| 프레임워크 | **Next.js 16** (App Router + Turbopack) |
| 언어 | **TypeScript** |
| 데이터베이스/인증 | **Supabase** (PostgreSQL + Google OAuth) |
| 스타일링 | **Tailwind CSS v4** |
| UI 컴포넌트 | **shadcn/ui** (Radix UI 기반) |
| 애니메이션 | **Framer Motion** |
| 아이콘 | **Lucide React** |
| 날짜 처리 | **date-fns** (KST 기준) |
| 테마 | **next-themes** (다크/라이트 모드) |

### 왜 이 스택인가?

혼자서 빠르게 개발해야 하는 상황이었기 때문에 **BaaS(Backend as a Service)** 인 Supabase를 선택했다. 인증, 데이터베이스, Row-Level Security까지 한 곳에서 해결할 수 있어서 별도의 백엔드 서버 없이도 안전한 앱을 만들 수 있었다.

Next.js의 **Server Actions** 패턴을 사용해 API 라우트를 따로 만들지 않고도 서버 사이드 로직을 처리했다. 기존 REST API 방식보다 코드량이 크게 줄었다.

---

## 핵심 기능

### 1. 기간(Period) 관리

6개월 같은 단위로 "기간"을 생성하고, 그 안에서 목표를 설정한다. 친구를 이메일로 초대하면 같은 기간을 공유하게 된다.

<!-- 📸 이미지 제안: 대시보드 화면 - 기간 카드 목록이 보이는 /app 페이지 스크린샷 -->

### 2. 세 가지 목표 타입

OurLife의 가장 핵심적인 기능은 **세 가지 타입의 목표**를 지원한다는 것이다.

#### 채우기 (ROUTINE)

습관을 만들거나 횟수를 채워가는 목표다. "이번 기간 동안 운동 50회", "매주 독서 3회"처럼 설정할 수 있다. 전체 기간, 주간, 월간 주기를 선택할 수 있고, 프로그레스바로 진행률을 확인한다.

<!-- 📸 이미지 제안: ROUTINE 타입 GoalCard 스크린샷 (파란색 테마, 프로그레스바 포함) -->

#### 아껴쓰기 (LIMIT)

제한을 두는 목표다. "주간 스크린타임 20시간 이하", "월간 배달음식 10회 이하" 같은 소비/습관 제한에 적합하다. 한도가 20 이하일 때는 **세그먼트 바(칸 나누기)** 로 남은 횟수를 직관적으로 보여준다.

<!-- 📸 이미지 제안: LIMIT 타입 GoalCard 스크린샷 (주황색 테마, 세그먼트 바 포함) -->

#### 도달하기 (OBJECTIVE)

최종 목표를 향해 공부량을 쌓아가는 타입이다. "학점 4.5 달성"처럼 결과 목표를 설정하고, 매일 공부한 시간을 기록하며 누적한다. 세부 과목(서브카테고리)도 설정할 수 있어서 어떤 분야를 공부했는지 추적할 수 있다.

<!-- 📸 이미지 제안: OBJECTIVE 타입 GoalCard 스크린샷 (보라색 테마, 서브카테고리 뱃지 포함) -->

### 3. 일일 기록

매일 일기를 쓰고, 설정한 목표들의 달성 여부를 체크한다. LIMIT 목표는 오늘 사용한 횟수를, OBJECTIVE 목표는 오늘 공부한 시간을 입력한다. 저장하지 않고 페이지를 벗어나려 하면 경고가 뜨는 안전장치도 있다.

<!-- 📸 이미지 제안: 일일 기록 페이지 스크린샷 - 일기 작성 영역 + 목표 체크 영역 -->

### 4. 노션 스타일 캘린더

기간 상세 페이지에는 노션처럼 월간 캘린더가 있다. 각 날짜에 누가 기록을 남겼는지 아바타로 표시되고, 날짜를 클릭하면 그날의 일기와 달성한 목표를 팝업으로 볼 수 있다.

<!-- 📸 이미지 제안: PeriodCalendar 컴포넌트 스크린샷 - 월간 캘린더 + DayDetailModal 팝업 -->

### 5. 다크 모드

next-themes를 사용해 다크/라이트 모드 전환을 지원한다. 모든 컴포넌트가 두 테마에 맞게 스타일링되어 있다.

<!-- 📸 이미지 제안: 같은 페이지의 라이트 모드 vs 다크 모드 비교 (좌우 배치) -->

---

## 데이터베이스 설계

Supabase의 PostgreSQL을 사용하며, 총 5개의 테이블로 구성된다.

```
users ─────┐
           │
periods ───┤ (participant_ids[])
           │
goals ─────┤ (user_id, period_id)
           │
daily_logs ┤ (user_id, period_id, log_date)
           │
goal_logs ─┘ (daily_log_id, goal_id)
```

### 단일 목표 테이블 전략

세 가지 목표 타입(ROUTINE, LIMIT, OBJECTIVE)을 하나의 `goals` 테이블에서 관리한다. 타입별로 사용하는 필드가 다르지만, 공유 필드가 많아서 서브타입 테이블로 분리하는 것보다 단순하게 유지했다.

- **ROUTINE**: `target_count`, `current_count`, `cycle`
- **LIMIT**: `limit_value`, `monthly_limit`, `cycle`
- **OBJECTIVE**: `target_value`, `achieved_value`, `study_target`, `study_unit`, `subcategories` (JSONB)

### Row-Level Security (RLS)

모든 테이블에 RLS 정책이 적용되어 있어서, 같은 기간에 참여하는 사용자만 데이터를 조회하고 수정할 수 있다. 프론트엔드에서 아무리 조작해도 DB 레벨에서 차단된다.

---

## 프로젝트 구조

```
our-life/
├── src/
│   ├── app/                    # Next.js App Router 페이지 (8개 라우트)
│   │   ├── actions/            # Server Actions (period, goal, daily-log)
│   │   ├── periods/[periodId]/ # 기간 상세 + 개인 상세 + 일일 기록
│   │   └── auth/               # Google OAuth 로그인/콜백
│   ├── components/
│   │   ├── ui/                 # shadcn/ui 기본 컴포넌트
│   │   ├── periods/            # 기간 관련 (캘린더, 생성/수정 모달)
│   │   ├── goals/              # 목표 관련 (추가/수정 다이얼로그)
│   │   ├── goal-card.tsx       # 타입별 목표 카드 (핵심 컴포넌트)
│   │   └── daily-log-form.tsx  # 일일 기록 폼
│   ├── lib/
│   │   ├── supabase/           # Supabase 클라이언트 (server/client/admin)
│   │   ├── goal-calculator.ts  # 목표 달성률 계산 로직
│   │   └── date-utils.ts       # KST 타임존 유틸리티
│   └── types/index.ts          # TypeScript 인터페이스
├── supabase/schema.sql         # DB 스키마 + RLS 정책
└── docs/                       # 프로젝트 문서 (AI 협업용)
```

---

## 바이브 코딩 과정에서 배운 것들

### AI와의 협업 구조

이 프로젝트의 `docs/` 폴더에는 AI에게 컨텍스트를 전달하기 위한 문서들이 있다.

- **STATUS.md**: 현재 진행 상황 요약 (AI가 작업 시작 전 읽는 파일)
- **SCHEMA.md**: 데이터베이스 ERD와 테이블 상세
- **ROUTES_UI.md**: 페이지 구조와 UI 요구사항
- **DECISIONS.md**: 기술적 의사결정 로그
- **CHANGELOG.md**: 일자별 변경 내역

이런 문서를 잘 관리하면 AI가 프로젝트의 맥락을 빠르게 파악하고, 일관된 방향으로 코드를 작성할 수 있다. 바이브 코딩에서 가장 중요한 것 중 하나가 **AI에게 줄 컨텍스트 관리**라고 느꼈다.

### 의사결정을 기록하라

`DECISIONS.md`에는 "왜 Supabase를 선택했는가", "왜 Goal 테이블을 하나로 합쳤는가", "왜 KST 기준으로 날짜를 계산하는가" 같은 의사결정이 기록되어 있다. AI에게 같은 질문을 반복하지 않아도 되고, 나중에 다른 방향으로 수정하더라도 당시의 맥락을 되짚을 수 있다.

### 하루 만에 MVP 완성

CHANGELOG를 보면 2026-02-01 하루 동안 프로젝트 초기화부터 전체 기능 구현, UI 고도화까지 완료했다. 기존에 이런 규모의 프로젝트를 혼자 개발하려면 최소 1~2주는 걸렸을 것이다. AI와 함께하니 반복적인 코드 작성, 스타일링, 에러 핸들링 등에 들어가는 시간이 크게 줄었다.

---

## 기술적으로 흥미로운 포인트

### KST 타임존 처리

한국 사용자 대상 서비스이기 때문에 모든 "오늘" 판별을 서울 시간(KST, Asia/Seoul) 기준으로 한다. UTC 기반으로 처리하면 자정 전후로 날짜가 하루 밀리는 문제가 생기는데, 이를 방지하기 위해 `date-utils.ts`에서 KST 변환 함수를 별도로 구현했다.

### Server Actions 패턴

Next.js 16의 Server Actions를 활용해 폼 제출 처리를 한다. API 라우트를 별도로 만들 필요 없이 서버 함수를 직접 호출하는 방식이라 코드가 간결하다. RLS를 우회해야 하는 경우에만 Supabase의 Service Role Key를 사용하는 Admin 클라이언트를 사용한다.

### 목표 달성률 계산

`goal-calculator.ts`가 이 프로젝트의 핵심 로직을 담당한다. 목표 타입(ROUTINE/LIMIT/OBJECTIVE)과 주기(전체/주간/월간)의 조합에 따라 달성률 계산 방식이 달라진다. 예를 들어 주간 ROUTINE 목표는 현재 주의 기록만 집계하고, 월간 LIMIT 목표는 현재 월의 사용량을 한도와 비교한다.

### Framer Motion 애니메이션

단순한 기능 구현을 넘어서 사용자 경험을 고려한 애니메이션을 적용했다. 페이드인, 순차 등장(Stagger), 호버 효과 등이 자연스럽게 적용되어 있다. `motion-layout.tsx`에 재사용 가능한 애니메이션 래퍼 컴포넌트를 만들어두고 전체 앱에서 일관되게 사용한다.

---

## 앞으로의 계획

현재 MVP가 완성된 상태이며, 아래 기능들을 추가로 고려하고 있다.

- 실시간 알림 (Supabase Realtime 활용)
- 통계/분석 대시보드 (목표별 달성 추이 그래프)
- 모바일 최적화 강화
- PWA 지원

---

## 마치며

OurLife는 "친구와 함께 성장하자"는 단순한 아이디어를 기술로 구현한 프로젝트다. 바이브 코딩의 장점을 살려 AI와 대화하면서 빠르게 MVP를 만들어냈고, 기술적으로도 Next.js Server Actions, Supabase RLS, 타임존 처리 등 실무에서 자주 마주치는 문제들을 다뤘다.

코드 전체는 [GitHub 레포지토리](https://github.com/hwankr/our-life)에서 확인할 수 있다.

---

## 이미지 가이드

본문에 `<!-- 📸 이미지 제안 -->` 주석으로 표시한 위치에 스크린샷을 삽입하면 된다. 아래는 각 이미지를 촬영하는 방법이다.

### 필요한 스크린샷 목록

| # | 위치 | 촬영 대상 | 방법 |
|---|------|-----------|------|
| 1 | 프로젝트 소개 | 랜딩 페이지 | `localhost:3000`에서 라이트/다크 모드 각각 캡처 |
| 2 | 기간 관리 | 대시보드 | `/app` 페이지에서 기간 카드가 보이는 상태로 캡처 |
| 3 | 채우기(ROUTINE) | GoalCard | `/periods/[id]/users/[userId]`에서 파란색 ROUTINE 카드 캡처 |
| 4 | 아껴쓰기(LIMIT) | GoalCard | 같은 페이지에서 주황색 LIMIT 카드 (세그먼트 바 포함) 캡처 |
| 5 | 도달하기(OBJECTIVE) | GoalCard | 같은 페이지에서 보라색 OBJECTIVE 카드 캡처 |
| 6 | 일일 기록 | DailyLogForm | `/periods/[id]/users/[userId]/logs/[date]` 페이지 캡처 |
| 7 | 캘린더 | PeriodCalendar | `/periods/[id]` 페이지에서 캘린더 + 팝업 캡처 |
| 8 | 다크 모드 비교 | 아무 페이지 | 같은 페이지를 라이트/다크 모드로 각각 캡처 후 좌우 병렬 배치 |

### 스크린샷 촬영 팁

1. **브라우저 DevTools** (`F12`) > 디바이스 에뮬레이션으로 일정한 뷰포트 크기 설정 (추천: 1280x800 또는 390x844 모바일)
2. **macOS**: `Cmd + Shift + 4 + Space` 로 윈도우 단위 캡처
3. **Windows**: `Win + Shift + S` 로 영역 캡처
4. **Chrome 전체 페이지 캡처**: DevTools > `Cmd+Shift+P` > "Capture full size screenshot"
5. 라이트/다크 비교 이미지는 [Figma](https://figma.com)나 [Canva](https://canva.com)에서 좌우 배치 후 하나의 이미지로 내보내기

### 대안: 스크린샷 없이 운영하기

아직 배포 전이라 실제 데이터가 없다면:

1. **더미 데이터 생성**: 로컬에서 앱을 실행하고 테스트 계정으로 목표와 기록을 몇 개 입력한 뒤 캡처
2. **코드 스니펫으로 대체**: 이미지 대신 핵심 코드 블록을 삽입 (이미 본문에 일부 포함)
3. **아키텍처 다이어그램**: [Excalidraw](https://excalidraw.com)나 [draw.io](https://draw.io)로 시스템 구성도를 그려서 대체
